# makemore

makemore is a bigram character-level language model that will gradually build up into a modern Transformer language model (think GPT). 

This project focuses on getting comfortable with `torch.Tensor` and the core mechanics of language modeling - training neural networks, sampling from them, and evaluating loss functions like negative log likelihood.

Credit: [Andrej Karpathy](https://twitter.com/karpathy)